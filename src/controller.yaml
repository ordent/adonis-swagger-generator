{{#each endpoints}}
{{this.routes}}:
  {{#each this.method}}
  {{this.method}}:
    tags: 
      - {{this.tags}}
    description: "{{state this.description}}"
    produces:
      - application/json
    parameters:
    {{#each this.path}}
    - name: {{this}}
      in: path
      type: integer
    {{/each}}
    responses:
      200:
        description: "{{state this.description}}"
  {{/each}}
{{/each}}