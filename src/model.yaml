definitions:
  {{model}}:
    type: object
    properties:
      {{#each properties}}
      {{this.name}}:
        type: {{this.type}}
        {{#if this.format}}
        format: {{this.format}}
        {{/if}}
        description: {{this.description}}
      {{/each}}
